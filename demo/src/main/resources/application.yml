spring:
  application:
    name: gothdb-demo
  datasource:
    url: jdbc:h2:mem:gothdb_demo;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: always

gothdb:
  enabled: true
  path: /gothdb

server:
  port: 8080
